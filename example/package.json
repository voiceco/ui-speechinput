{
  "name": "example",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "deploy": "now && now alias $(pbpaste) speechui.boswell.ai",
    "start": "node index.js",
    "start-dev": "npm run watch & node index.js --https",
    "watch": "watchify client.js -v -o public/js/bundle.js --verbose"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.16.1",
    "watson-developer-cloud": "^2.40.0"
  },
  "devDependencies": {
    "watchify": "^3.9.0"
  }
}
